# SDD Validation Report — Growth / Anti-stub Batch

**Дата:** 2026-08-09  
**Метод:** Subagent-Driven Development (Spec Reviewer → Verifier → Code Reviewer)  
**Ветка:** `main` @ `508e7bd` (synced with origin)  
**Workspace:** Danila master worktree  

---

## 1. Scope

| Commit | Title |
|--------|--------|
| `38e669a` | anti-stub honesty waves |
| `c88162c` | pytest / pytest-asyncio in requirements |
| `51b2829` | matcher SQL prefilter + catalog indexes + full CI pytest |
| `190fd41` | multi-worker budget, classify cache, golden reports, health, schemas |
| `508e7bd` | gitignore golden regression exports |

---

## 2. SDD Roles

| Role | Agent | Verdict |
|------|--------|---------|
| **Spec Reviewer** | feature-dev:code-reviewer | Goals: 5 PASS / 4 PARTIAL / 0 FAIL · **DoD 78/100** |
| **Verifier** | quality-engineer | **380 passed**, 1 skipped · Alembic head OK · tsc OK |
| **Code Reviewer** | feature-dev:code-reviewer | **REQUEST_CHANGES** (4 blockers) |

---

## 3. Spec Review — Goal matrix

| ID | Goal | Verdict | Notes |
|----|------|---------|--------|
| S-MATCH | SQL prefilter + cap | **PASS** | tokens + `ilike` + `MAX_CATALOG_CANDIDATES` |
| S-OEM-IDX | indexes | **PASS** | migration `7c2e9a1b0d40` |
| D-CI | full pytest CI | **PASS** | `pytest tests/` in GHA |
| S-BUDGET | multi-worker budget | **PARTIAL** | DB load exists; model-key mismatch vs concrete LLM names |
| S-LLM | classify + cache | **PASS** | wire-up in intake classifier |
| I-GOLD | report + threshold | **PASS** (thin) | JSON + log alert; no pager |
| A-GOD | schemas slice | **PASS** | `request_schemas.py` |
| R-OBS | /health checks | **PARTIAL** | ERP on every probe; worker not live-checked |
| Anti-stub | honesty | **PASS** | PO LOCAL_DRAFT, SmartMatch empty, portal |

**Overall DoD (spec):** **78/100**

---

## 4. Validation Evidence

| Check | Result |
|-------|--------|
| Targeted suite (matcher, llm, stubs, engines, portal, agents, api, learning) | **107 passed** |
| Full `tests/` | **380 passed, 1 skipped** |
| `alembic upgrade head` (SQLite) | **OK → `7c2e9a1b0d40`** |
| `admin_cockpit` `tsc --noEmit` | **OK** |
| Failed tests | **none** |

Env: `TESTING=1 PARTSOPS_BUDGET_USE_DB=0` for pytest.

**VALIDATION_GAP:** Postgres production migration not exercised in this run (SQLite only).

---

## 5. Code Review — Findings

### Critical / blockers (REQUEST_CHANGES)

1. **Matcher prefilter + LIMIT without ORDER BY** — can drop fuzzy near-misses and arbitrary-slice large catalogs; empty prefilter → `[]` with no fallback.  
2. **Offer re-validation via top-K matcher** (`request_service`) — selected `catalog_id` may fail 422 if outside prefiltered top-K; should load by PK.  
3. **`/health` calls live ERP** — bad for k8s liveness; split live vs ready.  
4. **Budget keys** — enforcement often on concrete model names not in `DEFAULT_CONFIGS` → unlimited; multi-worker DB under-count risk with `max(db,mem)` and stats-without-model.

### High / Medium

- B-tree indexes weakly help `ILIKE '%token%'`.  
- Prompt cache: no tests; cache hits skip usage logging.  
- Golden regression path under-tested.  
- Health shape change may break clients expecting flat `{status: healthy}`.

### What is solid

- Anti-stub honesty paths and tests.  
- N+1 supplier preload in matcher.  
- Health does not leak secrets.  
- Migration table name `suppliercatalogitem` correct.  
- CI migration round-trip + full pytest.

---

## 6. Consolidated verdict

| Dimension | Score | Comment |
|-----------|------:|---------|
| Automated tests | 95 | Green full suite |
| Spec coverage (code present) | 90 | All goals started |
| Spec fidelity (behavior) | 72 | Budget / health / matcher recall |
| Review severity | — | **REQUEST_CHANGES** before “done-done” |
| Ship risk | Medium | Tests pass; production edge cases real |

**Ship recommendation:**  
- **OK as iterative main** already pushed (batch is live).  
- **Not OK** to mark growth batch closed without P0 fixes below.

---

## 7. P0 follow-ups (ordered)

1. Matcher: OEM-strong prefilter only + fallback when empty/low candidates; deterministic cap.  
2. Select-offer: DB lookup by `catalog_id` + tenant, not matcher top-K.  
3. Health: `/health` or `/health/live` lightweight; ready path for ERP with cache/timeout.  
4. Budget: canonical key (alias/family) + DB sum as SoT when DB ok; tests multi-worker.

**P1:** golden regression=true test; prompt-cache tests; trgm/normalized OEM; continue A-GOD on `contract_operations`.

---

## 8. Agent IDs (resume)

| Role | subagent_id |
|------|-------------|
| Spec | `019fe5c4-e447-7e63-8ec4-2d95dc03423c` |
| Code | `019fe5c4-e447-7e63-8ec4-2daebd400b9a` |
| Verify | `019fe5c4-e453-7721-b5e9-89f6c3e2d35e` |

---

*Generated by SDD orchestration, 2026-08-09.*

---

## 9. Re-review (same day, no P0 fix commits)

**Reviewer:** `019fe5d4-1e8f-7181-8b7c-816a289ee023`  
**HEAD:** `508e7bd` (unchanged)  
**Targeted tests re-run:** 63 passed (matcher, llm, stubs, api)

| # | Prior blocker | Re-review status |
|---|---------------|------------------|
| 1 | Matcher LIMIT / no fallback | **STILL OPEN** |
| 2 | Offer via top-K matcher | **PARTIALLY FIXED** — `select_match` uses PK; **pricing** still top-K |
| 3 | `/health` live ERP | **STILL OPEN** |
| 4 | Budget model-key / multi-worker | **STILL OPEN** |

### Verdict (re-review): **REQUEST_CHANGES**

No credit for unfixed code. Green tests do not close production edge cases.

### Clarification on #2
- `RequestService.select_match` — already correct PK lookup (was fixed earlier / never broken on that path).
- Residual: pricing revalidate in `request_service` (~lines 75–84) still filters selected offer through `match_part_from_db(..., limit=25)`.

---

*Re-review appended 2026-08-09.*
